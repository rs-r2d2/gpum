"""Documentation drift tests (spec 007-github-pages-site).

These run offline, need no GPU, and are part of the default suite. They exist because the
failure mode of a documentation site is not a broken build — it is a page that still renders
perfectly while telling the reader something that stopped being true.
"""
